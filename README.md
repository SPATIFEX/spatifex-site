# Spatifex public site

Static-first Astro starter for `https://spatifex.com`.

## Setup

```bash
npm install
npm run verify
npm run dev
```

Commit the generated `package-lock.json` before the first deployment.

The example GitHub Pages workflow is stored outside `.github/workflows`. Review and pin action references before activation.
